# Exemplo de arquivo .env
# Copie para .env e preencha com seus valores

# Opção 1: Caminho para o arquivo Service Account JSON
FIREBASE_SERVICE_ACCOUNT_PATH=./service-account-key.json

# Opção 2: JSON completo do Service Account (como string)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"seu-projeto",...}

# ID do projeto Firebase (opcional, será lido do Service Account se não fornecido)
# FIREBASE_PROJECT_ID=seu-projeto-id

# Chave OpenRouteService (para cálculo de rota/ETA do motorista)
# ORS_API_KEY=sua_chave_openrouteservice

# Chave Google Gemini (para Assistente IA - chat, visão, OCR de escala)
# GEMINI_API_KEY=sua_chave_gemini
